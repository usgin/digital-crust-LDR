This directory contains

 .sql files that are PostGresQL create table queries to create tables from the CSV files exported from Excel
 
 .csv files-- dumped from Excel workbooks used to compile content; the Excel files are located at ./Classes, ./Definitions, ./ImplementationElemetns, and ./Links for different kinds of DTR elements.