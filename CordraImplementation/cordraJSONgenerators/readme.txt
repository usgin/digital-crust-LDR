These .sql files will generate a csv file with 2 columns:
identifier-- the suffix identifier for the DTR element; this should be appended on the host prefix for the registry it is loaded into to get HTTP URI

JSON-- a JSON object representing an instance of the DTR element ; file names indicate the contained element

Generated files for loading to Cordra are in the JSON2LoadinCordra folder.